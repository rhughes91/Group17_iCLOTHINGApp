@model IEnumerable<Group17_iCLOTHINGApp.Models.UserComment>

@{ ViewBag.Title = "Index";
				string currentUser = Group17_iCLOTHINGApp.Controllers.UserPasswordsController..CurrentUser(); }

<h2>Index</h2>

<table class="table">
	<tr>
		<th>
			@Html.DisplayNameFor(model => model.commentDate)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.commentDescription)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.Customer.customerName)
		</th>
		<th></th>
	</tr>

	@foreach (var item in Model)
	{
		if (item.Customer.customerID == currentUser || currentUser == "admin")
		{
<tr>
	<td>
		@Html.DisplayFor(modelItem => item.commentDate)
	</td>
	<td>
		@Html.DisplayFor(modelItem => item.commentDescription)
	</td>
	<td>
		@Html.DisplayFor(modelItem => item.Customer.customerName)
	</td>
</tr>}
			}

</table>
